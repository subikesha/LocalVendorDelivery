<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalVendorDelivery">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AdminActivity"
            android:exported="false"
            android:label="Admin Panel" />

        <activity
            android:name=".AdminRegisterVendorActivity"
            android:exported="false"
            android:label="Register New Vendor" />

        <activity
            android:name=".AdminAddProductActivity"
            android:exported="false"
            android:label="Add Products to Vendor" />

        <activity
            android:name=".AdminOverviewActivity"
            android:exported="false"
            android:label="System Status Overview" />

        <activity
            android:name=".AdminRegisterDeliveryActivity"
            android:exported="false"
            android:label="Add Delivery Partner" />

        <activity
            android:name=".CustomerActivity"
            android:exported="false"
            android:label="Customer Portal" />

        <activity
            android:name=".VendorActivity"
            android:exported="false"
            android:label="Vendor Portal" />

        <activity
            android:name=".DeliveryActivity"
            android:exported="false"
            android:label="Delivery Portal" />

    </application>

</manifest>